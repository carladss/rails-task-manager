<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= check_box("tag", tag.id, {checked: task.completed}) %><%= link_to task.title, task_path(task) %>
      <%= link_to "- 🗑", task_delete_path(task), method: :delete, data: { confirm: "Are you sure" }%></li>
  <% end %>
</ul>

<%= link_to "Add a new task", tasks_new_path %>
